import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Vendor Discovery Platform - AI-Powered Vendor Comparison',
  description: 'Find and compare the best vendors for your needs with AI-powered research and analysis',
  keywords: 'vendor discovery, vendor comparison, AI research, vendor shortlist',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  )
}
